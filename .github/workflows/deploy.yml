name: Deploy to Digital Ocean Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to Production Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -e
            echo "🚀 Starting deployment to app.aisaturn.co..."
            
            # Navigate to application directory
            cd /var/www/chatwoot
            
            # Pull latest code from GitHub
            echo "📥 Pulling latest code from GitHub main branch..."
            git fetch origin main
            git reset --hard origin/main
            
            # Load Ruby environment (rbenv) and set correct version
            export PATH="$HOME/.rbenv/bin:$PATH"
            eval "$(rbenv init -)"
            rbenv install 3.2.2 --skip-existing
            rbenv local 3.2.2
            
            # Load environment variables from .env.production
            # (Already configured on server - no need to pass secrets)
            source /var/www/chatwoot/.env.production || true
            
            # Install Ruby dependencies
            echo "📦 Installing Ruby gems (bundle install)..."
            bundle install --deployment --without development test
            
            # Install Node dependencies
            echo "📦 Installing Node packages (pnpm install)..."
            pnpm install --frozen-lockfile
            
            # Install WhatsApp Web microservice dependencies
            echo "📦 Installing WhatsApp Web microservice dependencies..."
            cd lib/whatsapp_web && npm install && cd ../..
            
            # Clean Vite cache to force fresh build
            echo "🧹 Cleaning Vite cache..."
            rm -rf tmp/vite-cache
            rm -rf public/vite-dev
            rm -rf public/packs/.vite
            
            # Precompile assets for production
            echo "🎨 Precompiling Vite and Rails assets..."
            RAILS_ENV=production BUILD_MODE=library bin/vite build --force
            RAILS_ENV=production bin/vite build --force
            RAILS_ENV=production bundle exec rails assets:precompile
            
            # Run database migrations
            echo "🗄️ Running database migrations..."
            RAILS_ENV=production bundle exec rails db:migrate
            
            # Cleanup old YAML configs that cause errors
            echo "🧹 Cleaning up old YAML installation configs..."
            PGPASSWORD=chatwoot_password_2025 psql -h localhost -U chatwoot -d chatwoot_production -c "DELETE FROM installation_configs WHERE value::text LIKE '---%%';" || true
            
            # Update installation configs (branding) from YAML without triggering onboarding
            echo "🎨 Updating AISATURN branding configs..."
            RAILS_ENV=production bundle exec rails installation:update_configs
            
            # NOTE: db:seed is DISABLED in production because it sets Redis onboarding flag
            # which forces the app into installation wizard mode!
            # Seeds should ONLY run in development/staging environments.
            
            # Clear Rails cache
            echo "🧹 Clearing Rails cache..."
            RAILS_ENV=production bundle exec rails cache:clear || true
            
            # Clear Sidekiq queues (optional, use with caution)
            # Uncomment if you want to clear pending jobs on each deploy
            # echo "🧹 Clearing Sidekiq queues..."
            # RAILS_ENV=production bundle exec rails runner "Sidekiq::Queue.all.each(&:clear)" || true
            
            # Restart Chatwoot services
            echo "🔄 Restarting Chatwoot web and worker services..."
            sudo systemctl restart chatwoot-web
            sudo systemctl restart chatwoot-worker
            
            # Restart WhatsApp Web microservice (only if systemd unit exists)
            if sudo systemctl list-unit-files | grep -q whatsapp-web.service; then
              echo "🔄 Restarting WhatsApp Web microservice..."
              sudo systemctl restart whatsapp-web
            else
              echo "⚠️ WhatsApp Web service not configured - skipping restart"
              echo "📝 Run: sudo cp deployment/systemd/whatsapp-web.service /etc/systemd/system/ && sudo systemctl enable whatsapp-web"
            fi
            
            # Restart Nginx to clear any stuck connections
            echo "🔄 Restarting Nginx..."
            sudo systemctl restart nginx
            
            # Wait for services to start
            sleep 3
            
            # Check service status
            echo "✅ Checking service status..."
            sudo systemctl status chatwoot-web --no-pager || true
            sudo systemctl status chatwoot-worker --no-pager || true
            
            # Verify WhatsApp Web microservice (if configured)
            if sudo systemctl list-unit-files | grep -q whatsapp-web.service; then
              echo "🔍 Checking WhatsApp Web service status..."
              sudo systemctl status whatsapp-web --no-pager
              
              echo "🔍 Testing WhatsApp Web health endpoint..."
              if curl -f http://localhost:3001/health; then
                echo "✅ WhatsApp Web health check passed"
              else
                echo "❌ WhatsApp Web health check failed - deployment aborted"
                exit 1
              fi
            fi
            
            echo "✅ Deployment completed successfully!"
            echo "🌐 Application is now live at https://app.aisaturn.co"
